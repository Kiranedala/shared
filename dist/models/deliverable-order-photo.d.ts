export declare class DeliverableOrderPhoto {
    id: string;
    stopRelId: string;
    orderNumber: string;
    photoRefString: string;
    base64Image: string;
    constructor(id?: string, stopRelId?: string, orderNumber?: string, photoRefString?: string, base64Image?: string);
}
