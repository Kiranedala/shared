export declare class GridRange {
    startColumn: number;
    startRow: number;
    columnsWide: number;
    columnsHigh: number;
    constructor(startColumn: number, startRow: number, columnsWide: number, columnsHigh: number);
}
