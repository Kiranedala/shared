export declare class CarouselService {
    constructor();
    next(modalId: string): void;
    prev(modalId: string): void;
    first(modalId: string): void;
    goTo(modalId: string, index: number): void;
}
